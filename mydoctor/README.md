# MyDoctor App (Expo)

App híbrido que consome o **mydoctor-api** (Django).

1. Copie `.env.example` para `.env`
2. `npm install`
3. `npx expo start`

Com `EXPO_PUBLIC_API_URL` definida, use **Abrir sistema web** para o painel Django completo.
